
import Alt from 'alt';
// import chromeDebug from 'alt-utils/lib/chromeDebug';

const alt = new Alt();
// chromeDebug(alt);


export default alt;

// Webpack caches the modules so the next time you import Alt,
// it will return the same instance again.
